# GenAI Coding Problems

This repository contains daily practice implementations for GenAI-related coding problems. Each file represents a single day's coding challenge and solution, focused on building essential skills for production AI applications.

## Contents

- **Day 1**: Token cost estimator using `tiktoken`. A utility to count tokens and estimate generation costs for models like GPT-4o.
- **Day 2**: *(Python implementation)*
- **Day 3**: *(Python implementation)*
- **Day 4**: *(Python implementation)*

## Setup & Requirements

Make sure you have Python installed. You may also need to install relevant dependencies depending on the day's challenge (e.g., `tiktoken`):

```bash
pip install -r requirements.txt # (If added later)
# Or manually install specific dependencies
pip install tiktoken 
```

## Running the Code

You can run the individual practice scripts using standard Python commands. For example:

```bash
python Day1.py
```
